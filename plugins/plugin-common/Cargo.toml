[package]
name = "plugin-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared pop-launcher / cosmic-launcher protocol types and helpers"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
