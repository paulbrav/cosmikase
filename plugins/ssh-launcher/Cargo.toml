[package]
name = "ssh-launcher"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pop!_OS launcher plugin for SSH host quick connect"

[dependencies]
plugin-common = { path = "../plugin-common" }
dirs = "6.0"
