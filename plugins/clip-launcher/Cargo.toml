[package]
name = "clip-launcher"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pop!_OS launcher plugin for clipboard history via cliphist"

[dependencies]
plugin-common = { path = "../plugin-common" }
serde_json = "1.0"
