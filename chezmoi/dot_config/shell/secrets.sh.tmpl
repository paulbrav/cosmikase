# API Keys - fetched from Bitwarden at chezmoi apply time
# Requires: bw login && export BW_SESSION="$(bw unlock --raw)"
#
# Usage:
#   1. Store API keys in Bitwarden as Login items (key in password field)
#   2. Or use custom fields for multiple keys in one item
#   3. Run: chezmoi apply
#
# Examples (add these to the template when bitwarden CLI is configured):
# export EXA_API_KEY="..."
# export ANTHROPIC_API_KEY="..."
# export OPENAI_API_KEY="..."

