#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════════════════════
# cosmikase-install - Japanese cyberpunk themed system installer
# ═══════════════════════════════════════════════════════════════════════════════
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(dirname "$SCRIPT_DIR")"
CONFIG_FILE="${CONFIG_FILE:-$REPO_DIR/cosmikase.yaml}"
LOG_FILE="/tmp/cosmikase-install-$(date +%Y%m%d-%H%M%S).log"

# ═══════════════════════════════════════════════════════════════════════════════
# Colors - Cyberpunk Palette
# ═══════════════════════════════════════════════════════════════════════════════
NEON_PINK="212"
NEON_CYAN="51"
NEON_PURPLE="99"
NEON_GREEN="82"
NEON_ORANGE="208"
NEON_RED="196"
DIM="241"

# ═══════════════════════════════════════════════════════════════════════════════
# Check for gum
# ═══════════════════════════════════════════════════════════════════════════════
if ! command -v gum &>/dev/null; then
    echo "gum not found. Installing..."
    go install github.com/charmbracelet/gum@latest
    export PATH="$HOME/go/bin:$PATH"
fi

# ═══════════════════════════════════════════════════════════════════════════════
# Helper Functions
# ═══════════════════════════════════════════════════════════════════════════════
banner() {
    local p="\033[38;5;${NEON_PURPLE}m"
    local r="\033[0m"
    
    clear
    echo ""
    echo -e "  ${p}╔════════════════════════════════════════════════════════════════════════════╗${r}"
    
    # Use gum to render the inner part of the banner (width 76 inside the borders)
    local inner_ascii
    inner_ascii=$(gum style --foreground "$NEON_PINK" --width 76 --align center \
        "░█████╗░░█████╗░░██████╗███╗░░░███╗██╗██╗░░██╗░█████╗░░██████╗███████╗" \
        "██╔══██╗██╔══██╗██╔════╝████╗░████║██║██║░██╔╝██╔══██╗██╔════╝██╔════╝" \
        "██║░░╚═╝██║░░██║╚█████╗░██╔████╔██║██║█████═╝░███████║╚█████╗░█████╗░░" \
        "██║░░██╗██║░░██║░╚═══██╗██║╚██╔╝██║██║██╔═██╗░██╔══██║░╚═══██╗██╔══╝░░" \
        "╚█████╔╝╚█████╔╝██████╔╝██║░╚═╝░██║██║██║░╚██╗██║░░██║██████╔╝███████╗" \
        "░╚════╝░░╚════╝░╚═════╝░╚═╝░░░░░╚═╝╚═╝╚═╝░░╚═╝╚═╝░░╚═╝╚═════╝░╚══════╝")

    # Split into lines and add borders
    while IFS= read -r line; do
        echo -e "  ${p}║${r}${line}${p}║${r}"
    done <<< "$inner_ascii"

    echo -e "  ${p}╠════════════════════════════════════════════════════════════════════════════╣${r}"

    local inner_text
    inner_text=$(gum style --width 76 --align center \
        "$(echo -e "\033[38;5;${NEON_CYAN}m⛩️  システム設定  ⛩️\033[0m")" \
        "$(echo -e "\033[38;5;${DIM}mSystem Configuration\033[0m")")

    while IFS= read -r line; do
        echo -e "  ${p}║${r}${line}${p}║${r}"
    done <<< "$inner_text"

    echo -e "  ${p}╚════════════════════════════════════════════════════════════════════════════╝${r}"
    echo ""
}

phase_header() {
    local num="$1"
    local total="$2"
    local title="$3"
    local subtitle="$4"
    
    local c="\033[38;5;${NEON_CYAN}m"
    local r="\033[0m"

    echo ""
    echo -e "  ${c}┌────────────────────────────────────────────────────────────────────────────┐${r}"
    
    local inner_phase
    inner_phase=$(gum style --width 76 --align left \
        "$(echo -e "  \033[38;5;${NEON_PINK}m[$num/$total] $title\033[0m")" \
        "$(echo -e "        \033[38;5;${DIM}m$subtitle\033[0m")")
        
    while IFS= read -r line; do
        echo -e "  ${c}│${r}${line}${c}│${r}"
    done <<< "$inner_phase"
    
    echo -e "  ${c}└────────────────────────────────────────────────────────────────────────────┘${r}"
}

run_ansible() {
    local tags="$1"
    local desc="$2"
    
    cd "$REPO_DIR/ansible"
    
    if gum spin \
        --spinner.foreground "$NEON_PINK" \
        --title.foreground "$NEON_CYAN" \
        --spinner dot \
        --title "  処理中... $desc" \
        -- bash -c "uv run ansible-playbook -i inventory.yml playbook.yml \
            -e 'config_file=$CONFIG_FILE' \
            --become-password-file '$ANSIBLE_BECOME_PASSWORD_FILE' \
            --tags '$tags' >> '$LOG_FILE' 2>&1"; then
        gum style --foreground "$NEON_GREEN" "  ✓ 完了"
        return 0
    else
        gum style --foreground "$NEON_RED" "  ✗ 失敗 - $LOG_FILE"
        return 1
    fi
}

fix_flatpak_icons() {
    # Fix Flatpak icons not appearing in launcher
    gum spin \
        --spinner.foreground "$NEON_PINK" \
        --title.foreground "$NEON_CYAN" \
        --spinner dot \
        --title "  処理中... Flatpak icons" \
        -- bash -c "
            if [[ -d /var/lib/flatpak/exports/share/icons/hicolor ]]; then
                sudo gtk-update-icon-cache -f /var/lib/flatpak/exports/share/icons/hicolor/ 2>/dev/null || true
                sudo gtk4-update-icon-cache -f /var/lib/flatpak/exports/share/icons/hicolor/ 2>/dev/null || true
            fi
            if [[ -d \$HOME/.local/share/flatpak/exports/share/icons/hicolor ]]; then
                gtk-update-icon-cache -f \"\$HOME/.local/share/flatpak/exports/share/icons/hicolor/\" 2>/dev/null || true
                gtk4-update-icon-cache -f \"\$HOME/.local/share/flatpak/exports/share/icons/hicolor/\" 2>/dev/null || true
            fi
        " >> "$LOG_FILE" 2>&1
    gum style --foreground "$NEON_GREEN" "  ✓ 完了"
}

success_banner() {
    local g="\033[38;5;${NEON_GREEN}m"
    local r="\033[0m"
    
    echo ""
    echo ""
    echo -e "  ${g}╔════════════════════════════════════════════════════════════════════════════╗${r}"
    
    local inner_success
    inner_success=$(gum style --foreground "$NEON_GREEN" --width 76 --align center \
        "██████╗  ██████╗ ███╗   ██╗███████╗ ██╗" \
        "██╔══██╗██╔═══██╗████╗  ██║██╔════╝ ██║" \
        "██║  ██║██║   ██║██╔██╗ ██║█████╗   ██║" \
        "██║  ██║██║   ██║██║╚██╗██║██╔══╝   ╚═╝" \
        "██████╔╝╚██████╔╝██║ ╚████║███████╗ ██╗" \
        "╚═════╝  ╚═════╝ ╚═╝  ╚═══╝╚══════╝ ╚═╝")

    while IFS= read -r line; do
        echo -e "  ${g}║${r}${line}${g}║${r}"
    done <<< "$inner_success"

    echo -e "  ${g}╠════════════════════════════════════════════════════════════════════════════╣${r}"
    
    local inner_text
    inner_text=$(gum style --width 76 --align center \
        "$(echo -e "\033[38;5;${NEON_CYAN}m🌸 インストール完了 🌸\033[0m")" \
        "$(echo -e "\033[38;5;${DIM}mInstallation Complete\033[0m")")

    while IFS= read -r line; do
        echo -e "  ${g}║${r}${line}${g}║${r}"
    done <<< "$inner_text"

    echo -e "  ${g}╚════════════════════════════════════════════════════════════════════════════╝${r}"
    echo ""
    echo ""
    gum style --foreground "$NEON_CYAN" "  次のステップ:"
    echo ""
    gum style --foreground "$DIM" "    cosmikase-theme <name>  テーマを変更"
    gum style --foreground "$DIM" "    cosmikase               メニュー"
    gum style --foreground "$DIM" "    $LOG_FILE"
    echo ""
}

error_banner() {
    local red="\033[38;5;${NEON_RED}m"
    local r="\033[0m"
    
    echo ""
    echo ""
    echo -e "  ${red}╔════════════════════════════════════════════════════════════════════════════╗${r}"
    
    local inner_error
    inner_error=$(gum style --width 76 --align center \
        "$(echo -e "\033[38;5;${NEON_RED}mエラーが発生しました\033[0m")" \
        "$(echo -e "\033[38;5;${DIM}mAn error occurred\033[0m")")

    while IFS= read -r line; do
        echo -e "  ${red}║${r}${line}${red}║${r}"
    done <<< "$inner_error"
    
    echo -e "  ${red}╚════════════════════════════════════════════════════════════════════════════╝${r}"
    echo ""
    gum style --foreground "$DIM" "  $LOG_FILE"
    echo ""
}

# ═══════════════════════════════════════════════════════════════════════════════
# Main Installation
# ═══════════════════════════════════════════════════════════════════════════════
main() {
    banner
    
    gum style --foreground "$DIM" "  設定: $CONFIG_FILE"
    echo ""
    
    if ! gum confirm \
        --prompt.foreground "$NEON_CYAN" \
        --selected.background "$NEON_PURPLE" \
        "  インストールを開始しますか?"; then
        gum style --foreground "$NEON_ORANGE" "  キャンセル"
        exit 0
    fi
    
    echo ""
    # Styled sudo input using gum header
    BECOME_PASS=$(gum input --password --header "$(echo -e "\033[38;5;${NEON_CYAN}m🔐 sudo パスワードを入力してください (Enter sudo password):\033[0m")")
    
    # Validate password
    if ! echo "$BECOME_PASS" | sudo -S true 2>/dev/null; then
        gum style --foreground "$NEON_RED" "  パスワードが違います"
        exit 1
    fi
    
    # Create secure temp file for ansible become password
    PASS_FILE=$(mktemp)
    chmod 600 "$PASS_FILE"
    echo "$BECOME_PASS" > "$PASS_FILE"
    trap 'rm -f "$PASS_FILE"' EXIT
    export ANSIBLE_BECOME_PASSWORD_FILE="$PASS_FILE"
    
    # Keep sudo alive
    while true; do echo "$BECOME_PASS" | sudo -S true 2>/dev/null; sleep 50; kill -0 "$$" || exit; done &
    
    {
        echo "=== COSMIKASE INSTALL LOG ==="
        echo "Started: $(date)"
        echo "Config: $CONFIG_FILE"
        echo ""
    } > "$LOG_FILE"
    
    # ─────────────────────────────────────────────────────────────────────────
    # Phase 1: Packages
    # ─────────────────────────────────────────────────────────────────────────
    phase_header "1" "6" "パッケージ" "APT, Flatpak, fonts"
    run_ansible "packages" "packages" || { error_banner; exit 1; }
    fix_flatpak_icons
    
    # ─────────────────────────────────────────────────────────────────────────
    # Phase 2: Runtimes  
    # ─────────────────────────────────────────────────────────────────────────
    phase_header "2" "6" "ランタイム" "Rust, Go, Zig, Node, Python, Julia"
    run_ansible "runtimes" "runtimes" || { error_banner; exit 1; }
    
    # ─────────────────────────────────────────────────────────────────────────
    # Phase 3: Ghostty
    # ─────────────────────────────────────────────────────────────────────────
    phase_header "3" "6" "ゴースティ" "Building terminal from source"
    run_ansible "ghostty" "ghostty" || { error_banner; exit 1; }
    
    # ─────────────────────────────────────────────────────────────────────────
    # Phase 4: Tools
    # ─────────────────────────────────────────────────────────────────────────
    phase_header "4" "6" "ツール" "AI tools, security apps"
    run_ansible "tools" "tools" || { error_banner; exit 1; }
    
    # ─────────────────────────────────────────────────────────────────────────
    # Phase 5: Web Apps
    # ─────────────────────────────────────────────────────────────────────────
    phase_header "5" "6" "ウェブ" "Desktop shortcuts"
    run_ansible "web" "web" || { error_banner; exit 1; }
    
    # ─────────────────────────────────────────────────────────────────────────
    # Phase 6: Dotfiles
    # ─────────────────────────────────────────────────────────────────────────
    phase_header "6" "6" "設定" "Shell, editor configs"
    run_ansible "dotfiles" "dotfiles" || { error_banner; exit 1; }
    
    # ─────────────────────────────────────────────────────────────────────────
    # Complete
    # ─────────────────────────────────────────────────────────────────────────
    success_banner
}

main "$@"
